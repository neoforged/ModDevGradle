plugins {
    id 'net.neoforged.moddev'
}

neoForge {
    neoFormVersion = "1.21-20240613.152323"

    accessTransformers = project.files("accesstransformer.cfg")

    runs {
        client {
            client()
        }
        server {
            server()
        }
    }
}
