
plugins {
    id 'net.neoforged.moddev'
}

neoForge {
    version = project.neoforge_version

    neoFormRuntime {
        // verbose = true
    }
}

dependencies {
    implementation(accessTransformers(interfaceInjectionData(project(':common'))))
}
