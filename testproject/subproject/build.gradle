
plugins {
    id 'net.neoforged.moddev'
}

neoForge {
    version = project.neoforge_version

    neoFormRuntime {
        // verbose = true
    }
}

dependencies {
    implementation(project(':'))
    accessTransformers(project(':'))
    interfaceInjectionData(project(':'))
}
