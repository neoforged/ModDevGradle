
plugins {
    id 'net.neoforged.neoforgegradle.moddev'
}

repositories {
    mavenLocal()
}

neoForge {
    version = project.neoforge_version

    runs {
        client {
            client()
        }
        data {
            data()
        }
        server {
            server()
        }
    }

    mods {
        testproject {
            sourceSet sourceSets.main
        }
    }
}
